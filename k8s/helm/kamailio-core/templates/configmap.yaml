apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.instanceName }}-config
  labels:
    app.kubernetes.io/name: kamailio-core
    app.kubernetes.io/instance: {{ .Values.instanceName }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
data:
  config.cfg: |
{{ include "kamailio-core.config" . | indent 4 }}
